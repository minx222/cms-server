model Post {
  id          String  @id @default(uuid())
  title       String
  text        String
  isPublished Boolean @default(false)
}


model Recipe {
	id String @id @default(uuid())
  title       	String
  description 	String
	creationDate  DateTime
  isPublished 	Boolean @default(false)
	ingredients 	String[]
}

